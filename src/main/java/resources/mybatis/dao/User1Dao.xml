<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.dao.User1Dao">
    <select id="findById" parameterType="int" resultType="mybatis.pojo.User1">

        insert into t_user values(1, 'yyf');
        rollback;
        select *from t_user where id = #{id};

    </select>

</mapper>